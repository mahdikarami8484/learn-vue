import { showCaption } from "./keyframes/showCaption"
import { unShowCaption } from "./keyframes/unShowCaption"

const captionKeyframe = {
    showCaption,
    unShowCaption
}

export {captionKeyframe}