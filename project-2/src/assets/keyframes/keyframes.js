import { refine } from "./refine"
import { alertOpen } from "./alertOpen"
import { alertClose } from "./alertClose"

const keyframes = {
    refine,
    alertOpen,
    alertClose
}

export { keyframes }